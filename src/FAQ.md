# ❓ Часто задаваемые вопросы (FAQ)

## 🚀 Деплой и запуск

### Q: Приложение не открывается в Telegram
**A:** Проверьте:
1. ✅ URL в @BotFather должен быть `https://` (не `http://`)
2. ✅ URL доступен в браузере
3. ✅ Telegram кеш: перезапустите приложение
4. ✅ Попробуйте на Telegram Desktop (там есть консоль)

### Q: Как открыть консоль в Telegram Desktop?
**A:** 
- **Windows/Linux**: `Ctrl + Shift + I` или `F12`
- **Mac**: `Cmd + Option + I`
- Консоль покажет ошибки JavaScript

### Q: Vercel показывает ошибку при деплое
**A:** Проверьте:
```bash
# Локально соберите проект
npm install
npm run build

# Если есть ошибки - исправьте их
# Затем снова задеплойте
```

### Q: Как обновить приложение после изменений?
**A:**
1. Измените код
2. Если используете Git:
   ```bash
   git add .
   git commit -m "Update"
   git push
   ```
3. Vercel автоматически задеплоит
4. Или в Vercel Dashboard нажмите "Redeploy"

---

## 🗄️ База данных

### Q: Данные не сохраняются
**A:** Проверьте:
1. ✅ Supabase подключен (должен быть файл `/utils/supabase/info.tsx`)
2. ✅ Откройте консоль браузера - есть ли ошибки?
3. ✅ Проверьте, что `import * as kv from '../utils/supabase/kv_store'` работает

### Q: Как посмотреть данные в базе?
**A:** 
1. Зайдите в [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. SQL Editor → выполните:
   ```sql
   SELECT * FROM kv_store_3d050cd2;
   ```

### Q: Как очистить все данные?
**A:**
```sql
-- В Supabase SQL Editor
DELETE FROM kv_store_3d050cd2;
```
⚠️ **Осторожно**: Это удалит ВСЕ данные!

### Q: Как удалить данные одного пользователя?
**A:**
```sql
-- Замените 123456 на user_id
DELETE FROM kv_store_3d050cd2 
WHERE key LIKE 'user:123456%' 
   OR key LIKE '%:123456:%';
```

---

## 👥 Реферальная система

### Q: Реферальная ссылка не работает
**A:** Проверьте:
1. ✅ `botUsername` в `/components/ReferralTab.tsx` изменен
2. ✅ Username без символа `@` (правильно: `"mybot"`, неправильно: `"@mybot"`)
3. ✅ Ссылка должна быть: `https://t.me/BOTUSERNAME?start=refUSERID`

### Q: Реферал не начислился
**A:** Проверьте:
1. Пользователь уже был зарегистрирован? (можно пригласить только новых)
2. Откройте консоль - есть ли ошибка в `processReferral()`?
3. Пользователь не может пригласить сам себя

### Q: Как изменить награду за реферала?
**A:** В `/lib/database.types.ts`:
```typescript
export const REWARDS = {
  REFERRAL: 500,  // ← Измените на нужное значение
  DAILY_LOGIN: 100,
}
```

### Q: Можно ли отследить, кто кого пригласил?
**A:** Да, в базе данных:
```sql
SELECT * FROM kv_store_3d050cd2 
WHERE key LIKE 'referral:%';
```

---

## 📝 Задания

### Q: Задания не загружаются
**A:** 
1. Проверьте консоль на ошибки
2. Убедитесь, что `initializeApp()` вызывается в `App.tsx`
3. Проверьте, что `getTasks()` работает

### Q: Как добавить новое задание?
**A:** См. `/CUSTOMIZATION.md` раздел "Добавление нового задания"

Кратко:
```typescript
// В /lib/api.ts, функция getDefaultTasks()
{
  id: 6,  // Уникальный ID
  title: "Новое задание",
  description: "Описание",
  reward: 500,
  type: "social",
  action_url: "https://t.me/channel",
  is_active: true,
  created_at: new Date().toISOString(),
}
```

### Q: Как отключить задание?
**A:** Измените `is_active: false` или удалите из массива

### Q: Задание выполняется повторно
**A:** Это не должно происходить. Проверьте:
1. В БД есть запись в `user_tasks:{userId}`?
2. `completed: true` для этого задания?

### Q: Как проверить подписку на канал?
**A:** Для этого нужен **backend с Bot API**:
```javascript
// Псевдокод (требует серверной части)
const chatMember = await bot.getChatMember(chatId, userId);
if (chatMember.status === 'member' || chatMember.status === 'administrator') {
  // Пользователь подписан
}
```
Это сложнее и требует серверного кода.

---

## 👤 Профиль и уровни

### Q: Уровень не повышается
**A:** Проверьте:
1. Достаточно ли опыта? (см. таблицу в `/lib/database.types.ts`)
2. Вызывается ли `updateUserExperience()`?
3. Консоль браузера - есть ошибки?

### Q: Как изменить систему уровней?
**A:** В `/lib/database.types.ts`:
```typescript
export const LEVELS = [
  { level: 1, required_exp: 0 },
  { level: 2, required_exp: 500 },  // ← Измените эти значения
  // ...
];
```

### Q: Баланс не обновляется
**A:**
1. Проверьте, что `updateUserBalance()` вызывается
2. Event `balanceUpdated` отправляется?
3. `ProfileTab` слушает этот event?

### Q: Как дать пользователю бонус вручную?
**A:** Через Supabase SQL Editor:
```sql
-- Узнать текущий баланс
SELECT value FROM kv_store_3d050cd2 
WHERE key = 'user:123456';

-- Обновить (замените 123456 на user_id)
-- И измените баланс в JSON
UPDATE kv_store_3d050cd2 
SET value = jsonb_set(value, '{balance}', '5000')
WHERE key = 'user:123456';
```

---

## 🎨 Дизайн и UI

### Q: Как изменить цвета?
**A:** См. `/CUSTOMIZATION.md` раздел "Изменение цветов"

В `/styles/globals.css`:
```css
.dark {
  --background: #ВАШЦВЕТ;
  --primary: #ВАШЦВЕТ;
  /* и т.д. */
}
```

### Q: Как изменить иконки вкладок?
**A:** В `/App.tsx` замените импорты:
```typescript
// Было
import { ListTodo, Users, UserCircle } from "lucide-react";

// Стало (пример)
import { Sparkles, Share2, User } from "lucide-react";
```

### Q: Шрифт слишком маленький/большой
**A:** В `/styles/globals.css`:
```css
:root {
  --font-size: 16px;  /* ← Измените (14px - меньше, 18px - больше) */
}
```

### Q: Как добавить светлую тему?
**A:** Приложение использует только dark theme для Telegram.
Но можно добавить переключатель:
```typescript
// В App.tsx
const [theme, setTheme] = useState('dark');
<div className={theme}>
```

---

## 🔐 Безопасность

### Q: Можно ли подделать user_id?
**A:** Нет. Telegram передает подписанные данные через `initData`. 
Подделать невозможно без знания секретного токена бота.

### Q: Как защититься от спама?
**A:** Добавьте rate limiting:
```typescript
// Пример: не более 10 заданий в час
const taskCount = await getTasksCompletedLastHour(userId);
if (taskCount > 10) {
  return { success: false, error: "Too many requests" };
}
```

### Q: Безопасно ли хранить данные в KV Store?
**A:** Да, Supabase KV защищен:
- Row Level Security (RLS)
- Доступ только через API ключ
- HTTPS шифрование

---

## 💰 Telegram Stars

### Q: Как начислять реальные Telegram Stars?
**A:** Используйте [Telegram Stars API](https://core.telegram.org/bots/payments#stars):
```javascript
// Создание счета
const invoice = await bot.createInvoiceLink({
  title: "Купить 1000 stars",
  description: "Пополнение баланса",
  payload: "stars_1000",
  currency: "XTR",  // Telegram Stars
  prices: [{ label: "1000 stars", amount: 1000 }],
});
```

### Q: Можно ли вывести Stars?
**A:** Нужна интеграция с Telegram Stars API для вывода.
Это требует:
1. Backend сервер
2. Bot API токен
3. Обработка платежей

---

## 🔧 Разработка

### Q: Как запустить локально?
**A:**
```bash
npm install
npm run dev
```
Откройте `http://localhost:5173`

⚠️ Telegram API не будет работать локально!
Используйте mock данные (автоматически).

### Q: Как отлаживать?
**A:**
1. **Telegram Desktop** - есть консоль (F12)
2. **Локально** - обычная разработка
3. **Логи**:
```typescript
console.log("Debug:", data);
```

### Q: TypeScript ошибки
**A:**
```bash
# Проверить типы
npm run build

# Или
npx tsc --noEmit
```

### Q: Как добавить новый npm пакет?
**A:**
```bash
npm install package-name
```
Затем импортируйте:
```typescript
import Something from 'package-name';
```

---

## 📱 Telegram специфика

### Q: Как получить фото пользователя?
**A:** Telegram Web App API не передает фото.
Нужно использовать Bot API:
```javascript
const photos = await bot.getUserProfilePhotos(userId);
```

### Q: Как отправить сообщение пользователю?
**A:** Через Bot API (требует backend):
```javascript
await bot.sendMessage(userId, "Привет!");
```

### Q: Haptic feedback не работает
**A:** 
- Работает только в Telegram (не в браузере)
- Работает только на мобильных устройствах
- В Telegram Desktop не работает

### Q: Как изменить цвет кнопки в Telegram?
**A:** Telegram автоматически применяет тему.
Вы не можете менять цвета кнопок меню бота.

---

## 🐛 Типичные ошибки

### Ошибка: "Cannot read property 'WebApp' of undefined"
**A:** Telegram SDK не загружен.
Проверьте `/index.html`:
```html
<script src="https://telegram.org/js/telegram-web-app.js"></script>
```

### Ошибка: "Failed to fetch"
**A:** 
1. Проблема с сетью
2. Supabase недоступен
3. CORS ошибка (не должно быть в Supabase)

### Ошибка: "kv.get is not a function"
**A:** Неправильный импорт:
```typescript
// Правильно:
import * as kv from '../utils/supabase/kv_store';

// Неправильно:
import kv from '../utils/supabase/kv_store';
```

### Ошибка: "This expression is not callable"
**A:** TypeScript ошибка типов. Проверьте:
1. Правильные ли типы?
2. Функция определена?
3. Нет ли опечаток?

---

## 📊 Аналитика

### Q: Как посмотреть статистику пользователей?
**A:** Supabase SQL Editor:
```sql
-- Всего пользователей
SELECT COUNT(*) FROM kv_store_3d050cd2 
WHERE key LIKE 'user:%';

-- Топ 10 по балансу
SELECT value->>'first_name' as name, 
       (value->>'balance')::int as balance
FROM kv_store_3d050cd2 
WHERE key LIKE 'user:%'
ORDER BY balance DESC
LIMIT 10;

-- Всего рефералов
SELECT COUNT(*) FROM kv_store_3d050cd2 
WHERE key LIKE 'referral:%';
```

### Q: Как добавить Google Analytics?
**A:** В `/index.html`:
```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_ID');
</script>
```

---

## 🎯 Оптимизация

### Q: Приложение медленно загружается
**A:**
1. Оптимизируйте изображения
2. Используйте code splitting
3. Включите кеширование

### Q: Как ускорить Supabase запросы?
**A:**
1. Используйте `mget` вместо множественных `get`
2. Кешируйте данные на frontend
3. Минимизируйте количество запросов

### Q: Слишком много re-renders
**A:** Используйте `useMemo` и `useCallback`:
```typescript
const memoizedValue = useMemo(() => calculateSomething(), [dep]);
const memoizedCallback = useCallback(() => doSomething(), [dep]);
```

---

## 📚 Дополнительные ресурсы

### Q: Где найти больше информации?
**A:**
- 📖 `/README.md` - Общая документация
- 🚀 `/DEPLOYMENT_GUIDE.md` - Деплой
- 🎨 `/CUSTOMIZATION.md` - Кастомизация
- ✅ `/CHECKLIST.md` - Чек-лист
- ⚡ `/QUICK_START.md` - Быстрый старт
- 🏗️ `/ARCHITECTURE.md` - Архитектура

### Q: Где искать помощь?
**A:**
1. Google ошибку
2. [Stack Overflow](https://stackoverflow.com/)
3. [Telegram Mini Apps Docs](https://core.telegram.org/bots/webapps)
4. [Supabase Docs](https://supabase.com/docs)

---

## 💡 Лучшие практики

### Q: Как тестировать приложение?
**A:**
1. ✅ Тестируйте на разных устройствах
2. ✅ Проверяйте в Telegram iOS и Android
3. ✅ Тестируйте все функции после изменений
4. ✅ Проверяйте консоль на ошибки

### Q: Как не потерять данные?
**A:**
1. 📝 Делайте резервные копии БД
2. 📝 Используйте Git для кода
3. 📝 Экспортируйте важные данные

### Q: Что делать перед production?
**A:**
- [ ] Убрать все `console.log`
- [ ] Проверить все ссылки (каналы, группы)
- [ ] Протестировать все функции
- [ ] Убедиться, что `botUsername` правильный
- [ ] Проверить награды и балансы
- [ ] Сделать backup БД

---

**Не нашли ответ? Проверьте другие документы или откройте консоль для отладки! 🔍**
