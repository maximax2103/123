# 🚀 Инструкция по деплою Telegram Mini App

## ✅ Исправленные проблемы

1. ✅ Исправлена ошибка сборки Rollup
2. ✅ Обновлен URL бота на `starsquestsbot`
3. ✅ Переработана архитектура: backend через API endpoints
4. ✅ Добавлены все необходимые конфигурационные файлы

## 📋 Пошаговая инструкция

### 1. Деплой на Vercel

#### Вариант A: Через Git (рекомендуется)

1. Инициализируйте Git репозиторий (если еще не сделано):
```bash
git init
git add .
git commit -m "Initial commit"
```

2. Создайте репозиторий на GitHub и запушьте код:
```bash
git remote add origin https://github.com/yourusername/your-repo.git
git branch -M main
git push -u origin main
```

3. Зайдите на [vercel.com](https://vercel.com)
4. Нажмите **"Add New Project"**
5. Импортируйте ваш GitHub репозиторий
6. Vercel автоматически определит настройки (Vite)
7. Нажмите **"Deploy"**

#### Вариант B: Через Vercel CLI

```bash
npm install -g vercel
vercel login
vercel
```

### 2. Деплой Supabase Edge Functions

```bash
# Убедитесь что у вас установлен Supabase CLI
npm install -g supabase

# Войдите в Supabase
supabase login

# Деплой функций
supabase functions deploy server --project-ref ahkxhskqgdsgkiwxuikm
```

### 3. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Stars Quest")
   - Введите username бота: **starsquestsbot** (или другое имя)
4. Сохраните полученный **Bot Token**

### 4. Настройка Mini App

1. В чате с @BotFather отправьте `/newapp`
2. Выберите вашего бота
3. Введите название приложения
4. Введите описание
5. Загрузите иконку (512x512 px)
6. Загрузите GIF/видео демо (опционально)
7. Введите **Web App URL**: `https://your-app.vercel.app` (URL из Vercel)
8. Выберите **Inline Mode**: No

### 5. Если вы использовали другое имя бота

Если вы НЕ использовали `starsquestsbot`, то нужно обновить файл `/components/ReferralTab.tsx`:

```typescript
// Найдите строку 26:
const botUsername = "starsquestsbot";

// Замените на:
const botUsername = "your_actual_bot_username";
```

Затем сделайте новый коммит и запушьте:
```bash
git add .
git commit -m "Update bot username"
git push
```

Vercel автоматически задеплоит новую версию.

### 6. Тестирование

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку меню или отправьте команду `/start`
3. Должно открыться Mini App

## 🔍 Проверка работоспособности

### Frontend (Vercel)
- ✅ Приложение загружается
- ✅ Отображаются 3 вкладки: Задания, Рефералы, Профиль
- ✅ Темная тема применяется

### Backend (Supabase)
Проверьте health endpoint:
```bash
curl https://ahkxhskqgdsgkiwxuikm.supabase.co/functions/v1/make-server-3d050cd2/health
```

Ожидаемый ответ:
```json
{"status":"ok"}
```

### Интеграция
- ✅ Создается профиль пользователя
- ✅ Загружаются задания
- ✅ Работает система рефералов
- ✅ Haptic feedback работает

## 🐛 Решение проблем

### Ошибка при сборке на Vercel

**Проблема**: `Error: Command "npm run build" exited with 1`

**Решение**: 
1. Убедитесь, что все файлы закоммичены
2. Проверьте `package.json` - должны быть все зависимости
3. Попробуйте локальную сборку: `npm install && npm run build`

### Backend не отвечает

**Проблема**: Ошибки при вызове API

**Решение**:
1. Проверьте что edge functions задеплоены:
```bash
supabase functions list --project-ref ahkxhskqgdsgkiwxuikm
```

2. Проверьте логи:
```bash
supabase functions logs server --project-ref ahkxhskqgdsgkiwxuikm
```

### Реферальные ссылки не работают

**Проблема**: Ссылки ведут на 404

**Решение**:
1. Убедитесь что username бота правильный
2. Проверьте формат ссылки: `https://t.me/starsquestsbot?start=ref123456`

### Mini App не открывается

**Проблема**: При нажатии на кнопку ничего не происходит

**Решение**:
1. Проверьте что URL в настройках бота правильный
2. Убедитесь что сайт доступен по HTTPS
3. Проверьте настройки CORS на Supabase

## 📱 Дальнейшая настройка

### Обновление заданий

Отредактируйте функцию `getDefaultTasks()` в `/supabase/functions/server/index.tsx`:

```typescript
{
  id: 1,
  title: "Подписаться на канал",
  description: "Подпишитесь на наш Telegram канал",
  reward: 500,
  type: "social",
  action_url: "https://t.me/your_channel", // ⬅️ Измените на ваш канал
  is_active: true,
  created_at: new Date().toISOString(),
}
```

После изменений задеплойте снова:
```bash
supabase functions deploy server --project-ref ahkxhskqgdsgkiwxuikm
```

### Настройка наград

Отредактируйте константы в `/lib/database.types.ts`:

```typescript
export const REWARDS = {
  REFERRAL: 500,        // ⬅️ Награда за реферала
  DAILY_LOGIN: 100,     // ⬅️ Награда за ежедневный вход
} as const;
```

## 🎉 Готово!

Ваш Telegram Mini App готов к работе! 

**Важные ссылки:**
- 🌐 Frontend: https://your-app.vercel.app
- ⚙️ Backend: https://ahkxhskqgdsgkiwxuikm.supabase.co
- 🤖 Bot: https://t.me/starsquestsbot

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи Vercel: https://vercel.com/your-username/your-project/deployments
2. Логи Supabase: https://supabase.com/dashboard/project/ahkxhskqgdsgkiwxuikm/logs
3. Консоль браузера (F12) при запуске Mini App
